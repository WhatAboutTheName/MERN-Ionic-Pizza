import { IonContent } from '@ionic/react';
import React from 'react';
import { Header } from '../../components/header';

export const Order = () => {
    return (
        <>
            <Header
                title={'Order'}
                key={'Order'}
            />
            <IonContent>
            </IonContent>
        </>
    );
}