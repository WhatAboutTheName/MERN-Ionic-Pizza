import { IonContent } from '@ionic/react';
import React from 'react';
import { Header } from '../../components/header';

export const LogIn = () => {
    return (
        <>
            <Header
                title={'LogIn'}
                key={'LogIn'}
            />
            <IonContent>
            </IonContent>
        </>
    );
}