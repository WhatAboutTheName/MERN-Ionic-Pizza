import { IonContent } from '@ionic/react';
import React from 'react';
import { Header } from '../../components/header';

export const SignUp = () => {
    return (
        <>
            <Header
                title={'SignUp'}
                key={'SignUp'}
            />
            <IonContent>
            </IonContent>
        </>
    );
}